<form method="post" action="{{ path('key_pair_delete', {'id': key_pair.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ key_pair.id) }}">
    <button class="btn">Delete</button>
</form>
